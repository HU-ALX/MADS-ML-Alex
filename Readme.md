# MADS-ML-ALEX

Assignment for MADS Machine Learning course.

## Goal

Tune a neural network on Fashion-MNIST and study the relationship between hyperparameters and validation accuracy.

## Setup

```bash
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -r requirements.txt